---
import { ProductDemo } from "./ProductDemo";
---

<section id="how-it-works" class="section demo-section" aria-labelledby="demo-title"><div class="shell">
  <div class="section-heading section-heading--split"><div><p class="section-label"><span class="lang-id">Bukti, bukan asumsi</span><span class="lang-en">Evidence, not assertion</span></p><h2 id="demo-title"><span class="lang-id">Lihat investigasi<br />terbentuk sendiri.</span><span class="lang-en">Watch the investigation<br />build itself.</span></h2></div><p><span class="lang-id">Replay adalah proyeksi dari event yang tersimpan. Node hanya muncul ketika observasinya benar-benar ada, dan inspector selalu menunjuk kembali ke bukti yang dipreservasi.</span><span class="lang-en">Replay is a projection of persisted events. Nodes appear only when the underlying observation exists, and the inspector always points back to preserved evidence.</span></p></div>
  <ProductDemo client:visible />
</div></section>
